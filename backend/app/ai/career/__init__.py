"""Shared, deterministic career-market datasets (salary benchmarks, etc.).

These are INTERNAL, curated, static datasets — never AI-generated and never
sourced from an external provider at request time. They ground advisory AI
narratives (salary benchmark tool, offer-negotiation guidance) so the model
paraphrases real numbers rather than inventing them.
"""
