import { PlatformOverviewScreen } from "@/components/admin/platform-overview-screen";

/**
 * Platform admin overview page (`/admin`).
 * Renders the cross-domain health snapshot via `PlatformOverviewScreen`.
 */
export default function AdminOverviewPage() {
  return <PlatformOverviewScreen />;
}
