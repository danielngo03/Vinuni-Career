import { CareerExploreScreen } from "./career-explore-screen";

/**
 * Public Career Explore — a real, guest-accessible discovery surface backed by
 * the live industry taxonomy and marketplace aggregates. See
 * `career-explore-screen.tsx`. No "coming soon" placeholder.
 */
export default function CareerExplorePage() {
  return <CareerExploreScreen />;
}
